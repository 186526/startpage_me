{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build;cp ./public/_dist_/sw.js ./public",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\"",
    "prod": "rm -rf ./public/;vercel --prod",
    "vercel-dev": "vercel dev;rm -rf ./public/"
  },
  "dependencies": {
    "@babel/preset-env": "^7.12.1",
    "@snowpack/plugin-optimize": "^0.2.7",
    "@snowpack/plugin-webpack": "^2.1.0",
    "raw-loader": "^4.0.2",
    "snowpack": "^2.16.1",
    "term-web": "^1.1.8"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
